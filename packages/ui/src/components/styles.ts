import { theme } from '@turborepo-smanetting/ui/src/theme';

export const buttonStyles = {
  backgroundColor: theme.colors.primary,
  padding: 10,
  borderRadius: 5,
  alignItems: 'center',
};

export const textStyles = {
  color: '#fff',
  fontSize: 16,
};
