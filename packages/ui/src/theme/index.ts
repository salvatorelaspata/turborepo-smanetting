import { colors } from './colors';

// Esportiamo tutte le variabili condivise da utilizzare nei componenti
export const theme = {
  colors,
  // Aggiungi altre variabili comuni come spacing, tipografia, ecc.
};